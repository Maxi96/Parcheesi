/// is_nest_empty(team)
/// @description check to see if the nest is empty
/// @param team
/// @return true if the nest is empty, otherwise false

var team;
team = argument0;

return (nest[team].occupants == 0);
